name: Release Binaries

on:
  push:
    branches: [ main ]
    tags:
      - "sql-hummus-v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-26.04
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Build release
        run: |
          set -euox pipefail
          cargo build --release

      - name: Prepare artifact
        run: |
          set -eoux pipefail
          mkdir -p dist
          cp target/release/sql-hummus dist/sql-hummus

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: sql-hummus-x86_64-unknown-linux-gnu-${{ GITHUB_SHA::8 }}
          path: dist/*
